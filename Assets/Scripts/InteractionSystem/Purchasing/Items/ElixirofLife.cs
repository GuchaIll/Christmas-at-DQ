using UnityEngine;

/*This item restores a small amount of karma, 
perfect for moments when karma balance is dangerously low
it's rare and expensive in trade*/

public class ElixirofLife : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}